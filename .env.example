# ============================================================================
# SISTEMA DE CONCILIACIÓN BANCARIA - CONFIGURACIÓN DE EJEMPLO
# ============================================================================
# Copia este archivo como .env y configura tus valores

# ====================================================================
# CONFIGURACIÓN DE BASE DE DATOS MYSQL
# ============================================================================
# Configuración principal de MySQL
DB_MSQL_USERNAME=root
DB_MSQL_PASSWORD=tu_password_mysql
DB_MSQL_DATABASE=alertadefinitivo
DB_MSQL_HOST=localhost
DB_MSQL_PORT=3306
DB_MSQL_DIALECT=mysql

# Variables legacy (para compatibilidad con código existente)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=alertadefinitivo
DB_USER=root
DB_PASSWORD=tu_password_mysql

# ============================================================================
# OPENAI API (REQUERIDA PARA OCR)
# ============================================================================
# Obtén tu API key desde: https://platform.openai.com/api-keys
# IMPORTANTE: Necesita acceso a Vision API para procesar PDFs bancarios
OPENAI_API_KEY=sk-tu-openai-api-key-aqui

# ============================================================================
# CONFIGURACIÓN DE APLICACIÓN
# ============================================================================
APP_NAME=Sistema de Conciliación Bancaria
APP_VERSION=1.0.0
DEBUG=true
SECRET_KEY=cambia-esta-clave-secreta-por-algo-mas-seguro

# ============================================================================
# CONFIGURACIÓN DE CORS (FRONTEND)
# ============================================================================
# Puertos del frontend (Next.js puede usar diferentes puertos)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:3002,http://127.0.0.1:3002

# ============================================================================
# CONFIGURACIÓN DE ARCHIVOS
# ============================================================================
# Configuración para subida de PDFs bancarios
MAX_FILE_SIZE=52428800          # 50MB en bytes
UPLOAD_FOLDER=uploads/
ALLOWED_EXTENSIONS=pdf          # Solo PDFs para estados de cuenta

# ============================================================================
# CONFIGURACIÓN ESPECÍFICA DE CONCILIACIÓN
# ============================================================================
# Tolerancia en montos para conciliación aproximada (en pesos mexicanos)
CONCILIACION_TOLERANCIA_MONTO=1.00

# Días de tolerancia para matching de fechas
CONCILIACION_DIAS_TOLERANCIA=3

# Tamaño máximo de archivo para conciliación (50MB)
CONCILIACION_MAX_FILE_SIZE=52428800

# ============================================================================
# CONFIGURACIÓN DE LOGS
# ============================================================================
# Nivel de logging: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# ============================================================================
# CONFIGURACIÓN AVANZADA (OPCIONAL)
# ============================================================================
# Configuraciones adicionales para fine-tuning del sistema

# Timeout para procesamiento OCR (segundos)
# OCR_TIMEOUT=120

# Número máximo de páginas a procesar por PDF
# MAX_PDF_PAGES=50

# Configuración de rate limiting para OpenAI
# OPENAI_RATE_LIMIT_RPM=60
# OPENAI_RATE_LIMIT_TPM=40000
